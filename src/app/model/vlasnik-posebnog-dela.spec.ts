import { VlasnikPosebnogDela } from './vlasnik-posebnog-dela';

describe('VlasnikPosebnogDela', () => {
  it('should create an instance', () => {
    expect(new VlasnikPosebnogDela()).toBeTruthy();
  });
});
